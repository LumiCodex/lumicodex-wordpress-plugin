# Internal AI-agent tooling (symlinks into private sibling repos, and
# workspace-only instructions; not relevant to the plugin and not meant
# to be public).
.claude/
.agents/
AGENTS.md
CLAUDE.md

# Local build output from bin/build-zip.sh (release zips are attached to
# GitHub Releases by the release workflow, not committed).
/build/
